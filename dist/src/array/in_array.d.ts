/**
 * 使用示例方法为：
 *   import FunArray from '@yioio/ts-fun/funArray'
 *   FunArray.inArray(xxx)
 *
 * 以下参数注释请到src/funArray.ts调用处查看
 * @param needle
 * @param haystack
 * @param strictMode
 *
 * return boolean
 */
export declare function inArray(needle: string | number | boolean, haystack: any, strictMode?: boolean): boolean;
export declare const _depends: string[];
